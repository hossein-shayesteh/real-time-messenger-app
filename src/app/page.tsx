import Button from "@/components/ui/Button";
import React from "react";

const Home = () => {
  return (
    <Button isLoading={true} size={"xs"}>
      dasda
    </Button>
  );
};

export default Home;
